---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<kbd
  class={`inline-flex h-5 min-w-[20px] items-center justify-center rounded-[5px] border border-rule bg-surface px-1.5 font-mono text-[11px] font-medium text-ink ${className}`}
>
  <slot />
</kbd>
