[package]
name = "jelly-mcp"
version = "0.1.0"
edition = "2021"

[dependencies]
jelly-core = { path = "../../jelly-core" }
tauri = { version = "2", features = [] }
axum = "0.8"
tokio = { version = "1", features = ["net", "sync"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
