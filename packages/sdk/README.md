# @jelly/sdk

The extension contract for Jelly — types and interfaces only, no implementation.
The kernel implements these; extensions consume them. Depends on nothing.

See [docs/extensions.md](../../docs/extensions.md) to author an extension.
